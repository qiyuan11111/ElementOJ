spring:
  application:
    name: elementoj-auth
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yml
        group: DEFAULT_GROUP
        namespace: 912999fd-8d07-40f5-be78-5c22ee6816a9
  config:
    import:
      - optional:nacos:common-mysql.yml?group=DATABASE
      - optional:nacos:common-security.yml?group=SECURITY
server:
  port: 8401
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# application.yml
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://elementoj-auth

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.security: TRACE
    org.springframework.security.oauth2: TRACE